import { ListDraggableDirective } from './list-draggable.directive';

describe('ListDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new ListDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
